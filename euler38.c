#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>

bool pandigital(long n) {
    bool *digits = calloc(10, sizeof(bool));
    while(n > 1) {
        
    }
}

int main() {
    
    return 0;
}
