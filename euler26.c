#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>

int main() {
    int val = 0;
    int len = 0;
    for (int i = 2; i < 1000; i++) {
        int *remainder = calloc(1000, sizeof(int));
        int count
        free(remainder);
    }
    return 0;
}
